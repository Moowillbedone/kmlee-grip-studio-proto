# 그립 스튜디오 2.0 · 한정판매 라이브 콘솔 — 프로토타입 가이드

> 라이브커머스 운영 콘솔(그립 스튜디오 2.0) 한정판매 화면의 **인터랙티브 프로토타입**.
> 화면 + 기획(PRD) + 자동 시연 데모를 한 파일에 담아, **기획 검증 · 공유 · 개발자 온보딩**에 쓰는 자산.

- **라이브**: https://kmlee-grip-studio-proto.vercel.app
- 단일 파일: `index.html` (+ `product.png`) · 브랜드 코랄 `#f5375b` · 폰트 Pretendard
- 사용법: 화면을 조작해보고 → 우하단 **⬤ PRD 모드**로 기획을 함께 보고 → 패널 상단 **▶ 자동 데모 보기**로 기능을 눈으로 확인

---

## 1. 화면 구성

좌측 사이드바 + 3컬럼:
- **방송 프리뷰**(좌): 라이브 송출 화면(타이머·상태바·오버레이) + 상품 카드 + 게임 5종
- **한정판매 패널**(중앙): 상품 리스트 / 가격 / 일괄 제어 — 핵심 작업 영역
- **실시간 성과 + 채팅**(우)

## 2. 주요 기능

- **상품 리스트 & 선택**: 6개 상품, 원형 체크박스(기본 회색 체크), 전체선택. 1개+ 선택 시 `[가격 수정]·스토어 ON/OFF` 활성화.
- **3단 가격 (정가 / 상시할인가 / 라이브가)**:
  - 3칸 모두 **리스트에서 인라인 직접 수정** (체크 불필요)
  - 상시할인가 입력 시 **할인율 배지 자동 계산** (정가 대비, 할인가≥정가면 0%)
  - 수정값은 **좌측 모바일 방송 오버레이에 실시간 반영**
  - Enter/blur 시 콤마 포맷 + **성공 토스트**(좌측 하단)
- **가격 일괄 수정 팝업**: `[가격 수정]` → 팝업(**열 때 항상 전체선택**). 대상(전체/정가/상시할인가/라이브가) 선택 + 금액 입력 → 반영.
  - **가격 최소 200원 검증(정가·상시할인가·라이브가 모두)**: 대상 중 하나라도 200원 미만이면 **실패 내역 팝업**(상품명·사유) 노출 + 해당 상품 **미적용** + 실패 토스트
- 판매상태(판매예정/판매시작/품절), 상품별 통계(장바구니·실주문·판매가능 수량·라이브 매출), 실시간 판매 성과, 채팅/공지/Q&A/FAQ/메모.

## 3. PRD 모드 — "살아있는 기획서" 패턴 ★

우하단 **⬤ PRD 모드** 토글 → 기획을 화면과 함께 매핑해서 보는 모드.

- 진입 시: 좌측 사이드바·방송 컬럼을 숨겨 공간을 확보(한정판매·성과·채팅 **컬럼 폭/비율은 그대로 유지**, 확보한 폭을 명세 패널이 흡수)하고, 우측 **명세 패널 슬라이드** + 화면 기능 위에 **번호 마커**.
- 패널 구성:
  1. **과제 개요** — 목적 / 배경·실제 문제 / 해결
  2. **검증 플로우** — 2가지 수정 경로 다이어그램 (아래)
  3. **번호별 기능 명세** — 각 항목 `정의 / (AS-IS / TO-BE) / 기능(불릿)`
- **양방향 연동**: 패널 항목 hover → 화면 번호 마커 glow / 화면 마커 클릭 → 패널 해당 항목으로 스크롤·활성화.
- 효과: 정적 문서 대신 **"이 화면의 이 요소 = 이 정책"** 을 한 화면에서 즉시 확인.

### 검증 플로우 — 2경로 구조 (중요)

가격 수정은 **서로 다른 2가지 경로**가 있고, 검증도 분리해서 해야 한다:

- **경로 A · 리스트 직접 수정 (체크 불필요)**: 정가·상시할인가·라이브가 입력 → Enter → 배지 자동계산·모바일 반영·성공 토스트
- **경로 B · 일괄 수정 (체크 + [가격 수정])**: 상품 1개+ 선택 → `[가격 수정]` 활성화·클릭 → 팝업(전체선택) 대상·금액 입력 → 반영 → **모든 가격 ≥ 200원?**(선택한 대상 전부) → (예) 적용+성공 / (아니오) 실패 팝업·미적용·실패 토스트
- 두 경로 모두 → 변경 결과 **비즈센터·스튜디오 데이터 연동** 반영

## 4. 자동 데모 — 풀스크린 가이드 투어 ★★ (핵심 자산)

> "정적 PRD/프로토타입은 *그래서 어떤 순서로 뭘 하는지* 전달이 약하다."
> 가이드 투어는 **실제 화면이 자동으로 동작**하면서 **어디서 무엇을 하는지** 콜아웃으로 짚어준다. 과정 + 결과를 눈으로 반복 학습 → 개발자/이해관계자 온보딩에 강력.

**동작**: PRD 패널 상단 `▶ 자동 데모 보기` → PRD 모드 자동 해제(**실제 큰 화면 전체**) → 화면 딤 + **현재 단계 요소에 스포트라이트 + 콜아웃** → **실제 UI를 자동 조작**(검증 플로우 2경로, 12단계)하며 **자동 반복**. 종료 시 원래 상태로 복구.

### 재사용 패턴 (어느 프로토타입에도 적용 가능)

1. **스포트라이트** = 타겟 rect 크기의 빈 div + `box-shadow: 0 0 0 9999px rgba(16,18,24,.62)` → 주변만 딤, 타겟만 하이라이트. (z-index를 모든 모달보다 높게)
2. **콜아웃** = 타겟 `getBoundingClientRect()` 기준으로 위치 계산한 말풍선(단계 태그 + 설명).
3. **실제 UI 자동 조작** = 가짜 화면이 아니라 **진짜 컴포넌트를 이벤트로 구동**:
   ```js
   el.click();                                            // 버튼/체크박스
   el.value = '3,000'; el.dispatchEvent(new Event('input',{bubbles:true}));   // 인풋 + 핸들러 트리거
   el.dispatchEvent(new KeyboardEvent('keydown',{key:'Enter',bubbles:true})); // Enter
   ```
   → 실제 토스트·팝업·배지·모바일 반영이 그대로 재현된다.
4. **단계 배열** `[{ tag, t:설명, target:()=>요소, act:()=>조작 }]` + `setInterval`로 진행, `idx=(idx+1)%steps.length` 로 **무한 반복**.
5. **스냅샷/복원**: 시작 시 상태를 스냅샷 → 각 루프 시작 & 종료 시 원복(체크 해제·값 복원·모달 닫기). 데모가 실제 데이터를 건드려도 깨끗하게 되돌린다.

> 구체 구현은 `index.html` 의 `// ===== 자동 데모 — 풀스크린 가이드 투어 =====` 블록 참고.

## 5. 디자인 토큰 (요약)

```
브랜드 코랄 --grip #f5375b / strong #ec2a4f / soft #ffeaef / soft-2 #fff2f4
배경 #f1f2f4 · 카드 #fff · 라인 #ececef · 필드라인 #e3e4e8
텍스트 #1f2126 > #5c606b > #8d919c · 라운드 카드14/버튼9 · 폰트 Pretendard
보조: 할인배지 글자#f5375b/배경#fdeaef · 중립버튼#8c9097 · 성공 그린#22c55e
```
전체 토큰·컴포넌트 스니펫은 별도 디자인 시스템 스킬(`grip-studio-design`)에 정리되어 있다.

## 6. 배포 / 운영

- **개인**: 이 레포 → Vercel 자동배포(`git push`만 하면 반영) → 위 라이브 URL.
- **사내**: 내부 GitHub Pages(조직 권한으로 열람)에도 동기화 배포.
- 변경분은 **양쪽에 동시 배포**해 일관성 유지. (지속 고도화 중인 자산)

---

## 부록 · 재사용 체크리스트

새 프로토타입에 이 자산의 강점을 이식하려면:
- [ ] 디자인 토큰(`:root`) + Pretendard CDN 적용 → 그립 톤 일관
- [ ] PRD 모드: 기능별 `{번호, 타겟 셀렉터, 정의/ASIS/TOBE/기능}` 정의 → 패널 + 화면 마커 양방향 연동
- [ ] 검증 플로우: 경로가 여러 개면 **분리**해서 다이어그램화 (직접 수정 vs 일괄 수정처럼)
- [ ] 자동 데모: 스포트라이트+콜아웃 엔진 재사용 + `act()`에서 **실제 컴포넌트를 dispatch로 구동** + 스냅샷/복원
